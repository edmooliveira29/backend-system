name: Backend-Clean

on:
  push:
    branches: [ main ]

jobs:
  test-unit:
    runs-on: ubuntu-latest
    steps:  
      - name: Unit testing
        uses: actions/checkout@v3
      - run: |
          npm install
          npm run test:unit
        with:
          path: path/to/coverage/report