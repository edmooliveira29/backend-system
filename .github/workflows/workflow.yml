name: api-backend
on:
  push:
    branches: [ main ]
jobs:
  # test-unit:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Start MongoDB v4.2
  #       uses: supercharge/mongodb-github-action@1.2.0
  #       with:
  #         mongodb-version: 4.2
  #     - name: Checkout
  #       uses: actions/checkout@v3
  #     - run: npm install
  #     - run: echo "MONGO_URL=${{vars.MONGO_URL}}" >> .env
  #     - run: npm run test:unit
  #     - name: Upload artifact
  #       uses: actions/upload-pages-artifact@v1
  #       with:
  #         path: './coverage'
  deploy:
    # needs: test-unit
    runs-on: ubuntu-latest
    steps:
      - id: 'auth'
        uses: 'google-github-actions/auth@v1'
        with:
          credentials_json: '${{ secrets.ACCOUNT_KEY_JSON_GOOGLE }}'

      - name: 'Set up Cloud SDK'
        uses: 'google-github-actions/setup-gcloud@v1'
          
      - name: Run commands via SSH
        run: | 
          echo "${{secrets.ACCOUNT_KEY_JSON_GOOGLE}}" > key.json
          echo key.json
          gcloud auth activate-service-account --key-file=key.json  
          gcloud compute ssh ${{secrets.NAME_INSTANCE_GOOGLE}} --zone=${{secrets.ZONE_INSTANCE_GOOGLE}} --command='ls && /bin/sh /home/${{secrets.USER_INSTANCE_GOOGLE}}/run-deploy.sh'
    # steps:
    #   - name: Set up Python
    #     uses: actions/setup-python@v2
    #     with:
    #       python-version: 3.7

    #   - name: Set up gcloud CLI
    #     uses: google-github-actions/setup-gcloud@v0.3.0
    #     with:
    #       service_account_key: ${{ secrets.ACCOUNT_KEY_JSON_GOOGLE }}
    #       project_id: ${{ secrets.PROJECT_ID_GOOGLE }}

    #   - name: SSH to GCP instance
    #     uses: google-github-actions/ssh-compute@v0.4.0
    #     with:
    #       project_id: ${{ secrets.PROJECT_ID_GOOGLE }}
    #       zone: ${{secrets.ZONE_INSTANCE_GOOGLE}} 
    #       instance_name: ${{secrets.NAME_INSTANCE_GOOGLE}}
    #       private_key: ${{ secrets.SSH_PRIVATE_KEY_GOOGLE }}
    #       username: ${{secrets.USER_INSTANCE_GOOGLE}}
    #       script: |
    #         echo "Hello, SSH!"
    #         gcloud compute ssh system --zone ${{secrets.ZONE_INSTANCE_GOOGLE}} --command='ls && /bin/sh /home/${{secrets.USER_INSTANCE_GOOGLE}}/run-deploy.sh' --troubleshoot




    # runs-on: ubuntu-latest
    # steps:
    # - name: Authenticate with gcloud and deploy
    #   env:
    #     GCP_SA_KEY: 
    #   run: |
    #     echo "${{ secrets.ACCOUNT_KEY_JSON_GOOGLE }}" > key.json
    #     gcloud auth activate-service-account --key-file=key.json --project=${{secrets.PROJECT_ID_GOOGLE}}
