name: api-backend

on:
  push:
    branches: [ main ]

jobs:
  # test-unit:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Start MongoDB v4.2
  #       uses: supercharge/mongodb-github-action@1.2.0
  #       with:
  #         mongodb-version: 4.2
  #     - name: Checkout
  #       uses: actions/checkout@v3
  #     - run: npm install
  #     - run: echo "MONGO_URL=${{vars.MONGO_URL}}" >> .env
  #     - run: npm run test:unit
  #     - name: Upload artifact
  #       uses: actions/upload-pages-artifact@v1
  #       with:
  #         path: './coverage'
  deploy:
    # needs: test-unit
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v0.4.0
        with:
          service_account_key: ${{ secrets.GCP_KEY }}
          project_id: api-8702109450667741338-992005

      - name: Access Google Instance
        run: |
          gcloud compute ssh system --zone us-east1-b --project api-8702109450667741338-992005 \
          --command="cd .. && cd edmooliveira29_gmail_com/project-system/backend-system/ && ls -a && npm run start-production
          # gcloud compute ssh system --zone us-east1-b --project api-8702109450667741338-992005 --command="ls -a && cd ~ && cd project-system/backend-system/ && ls -a && npm run start-production"
          # gcloud compute ssh system --zone us-east1-b --project api-8702109450667741338-992005 
          

