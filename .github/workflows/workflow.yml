name: api-backend

on:
  push:
    branches: [ main ]

jobs:
  test-unit:
    runs-on: ubuntu-latest
    steps:
      - name: Start MongoDB v4.2
        uses: supercharge/mongodb-github-action@1.2.0
        with:
          mongodb-version: 4.2
      - name: Checkout
        uses: actions/checkout@v3
      - run: npm install
      - run: echo "MONGO_URL=${{vars.MONGO_URL}}" >> .env
      - run: npm run test:unit
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: './coverage'
  deploy:
    needs: test-unit
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Authenticate with Google Cloud
        uses: google-github-actions/auth@v0
        with:
          credentials: ${{ secrets.GCP_KEY }}

      - name: Install SSH client
        run: sudo apt-get install -y openssh-client

      - name: Set up SSH key
        run: echo "${{ secrets.SSH_PRIVATE_KEY_GOOGLE }}" > ~/.ssh/id_rsa && chmod 600 ~/.ssh/id_rsa

      - name: SSH into instance
        run: |
          ssh -o "StrictHostKeyChecking=no" -i ~/.ssh/id_rsa edmooliveira29_gmail_com@34.139.16.117 "cd project-system/backend-system/ && ls -a && npm run start-production"
          gcloud compute ssh system --zone us-east1-b --project api-8702109450667741338-992005 --command ""
